
<!-- 01_Environment.sh -->
chmod +x 01_Environment.sh
bash 01_Environment.sh

<!-- 02_StartBackend.sh -->
chmod +x 02_StartBackend.sh
bash 02_StartBackend.sh

<!-- codebuddy命令行 -->
npm install -g @tencent-ai/codebuddy-code

codebuddy

<!-- merge 功能 -->

git log --merges --first-parent main --pretty=%B --no-notes > merge_msg.txt


mvn clean install

mvn clean package -DskipTests



cd 006-CampusHub

# gitingest 
gitingest /teamspace/studios/this_studio/006-CampusHub/shiqian-common -o shiqian-common.txt
gitingest /teamspace/studios/this_studio/006-CampusHub/shiqian-frontend -o shiqian-frontend.txt
gitingest /teamspace/studios/this_studio/006-CampusHub/shiqian-gateway -o shiqian-gateway.txt
gitingest /teamspace/studios/this_studio/006-CampusHub/shiqian-resource -o shiqian-resource.txt
gitingest /teamspace/studios/this_studio/006-CampusHub/shiqian-user -o shiqian-user.txt


# docker compose
docker compose up -d




# 1. 彻底杀掉所有后端
pkill -f 'shiqian-(user|resource|gateway)' || true
sleep 2

# 1.2 杀掉前端 Vite 开发服务
pkill -f 'vite' || true
pkill -f 'npm.*dev' || true
pkill -f 'pnpm.*dev' || true
pkill -f 'yarn.*dev' || true
sleep 2

ss -lntp | grep -E ':8080|:8081|:8082|:5173|:5174|:3000' || echo "后端和前端端口都已释放"

# 2. 必须执行的「干净打包」（关键！）
mvn clean package -DskipTests \
  -pl shiqian-common,shiqian-user,shiqian-resource,shiqian-gateway -am -B

# 3. 确认 MySQL 还活着
docker ps | grep shiqian-mysql

# 4. 重新启动三个服务
# （推荐用你自用.md 里的 nohup 方式，或者直接 java -jar）
# 示例：
nohup java -jar shiqian-user/target/shiqian-user-1.0.0-SNAPSHOT.jar \
  --spring.profiles.active=local \
  > logs/fresh-user.log 2>&1 &

nohup java -jar shiqian-resource/target/shiqian-resource-1.0.0-SNAPSHOT.jar \
  --spring.profiles.active=local \
  > logs/fresh-resource.log 2>&1 &

nohup java -jar shiqian-gateway/target/shiqian-gateway-1.0.0-SNAPSHOT.jar \
  --spring.profiles.active=local \
  > logs/fresh-gateway.log 2>&1 &


# SQL
# 发现乱码后一键修复
docker exec -i shiqian-mysql mysql -uroot -proot --default-character-set=utf8mb4 \
  -e "DROP DATABASE IF EXISTS shiqian_user; DROP DATABASE IF EXISTS shiqian_resource;"
docker exec -i shiqian-mysql sh -c 'mysql -uroot -proot --default-character-set=utf8mb4' < docker/mysql/init/init.sql
docker exec -i shiqian-mysql sh -c 'mysql -uroot -proot --default-character-set=utf8mb4' < docker/mysql/init/z-demo-data.sql
docker restart shiqian-redis          # 或 docker restart shiqian-resource

# 清理旧进程
pkill -f 'target/shiqian-(user|resource|gateway)' 2>/dev/null || true
pkill -f 'vite' 2>/dev/null || true
sleep 2
echo "✅ 已清理旧进程"
# 后端

### 启动 user 服务（前台）
java -jar shiqian-user/target/shiqian-user-1.0.0-SNAPSHOT.jar --spring.profiles.active=local --spring.cloud.nacos.discovery.enabled=true

### 启动 resource 服务（前台）
java -jar shiqian-resource/target/shiqian-resource-1.0.0-SNAPSHOT.jar --spring.profiles.active=local --spring.cloud.nacos.discovery.enabled=true

### 启动 gateway 服务（前台）
java -jar shiqian-gateway/target/shiqian-gateway-1.0.0-SNAPSHOT.jar --spring.profiles.active=local


# 前端
## 启动
cd 006-CampusHub
cd shiqian-frontend 
npm run dev

# 监控
## 检查端口是否启动成功（8080 / 8081 / 8082）
ss -tlnp | grep -E '8080|8081|8082'

## 同时查看三个服务的实时日志
tail -f logs/fresh-user.log logs/fresh-resource.log logs/fresh-gateway.log
