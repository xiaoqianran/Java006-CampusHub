# 时迁前端开发进度记录

> 记录前端重构的实际提交历史，便于后续继续开发

**最后更新**：2026-05-17（已切换到严格最小功能点流程）

---

## 当前分支

- `feature/frontend-v1-core-experience`

## 已保存的核心进度（本次 commit）

本次提交保存了前端重构的 **V1 核心体验**，包含以下已完成内容：

### 已实现功能
- 全新品牌化顶部导航 + 响应式布局（App.vue）
- 首页 Hero + 分类展示 + 最新资源推荐
- 完整的登录 / 注册页面（对接后端接口）
- 资源广场（分类树筛选 + 搜索 + 排序 + 分页 + 精美卡片）
- 资源详情页（下载、收藏、相关推荐）
- 4 步发布向导 + 世界级 FileDropzone（拖拽上传模拟）
- 个人中心（资料编辑 + 我的发布管理 + 我的收藏）
- 相关 Pinia Store（auth、category、favorites）
- Tailwind + 学术风设计系统
- Vite proxy 配置，便于本地联调

### 技术栈现状
- Vue 3 + TypeScript + Vite
- Element Plus + Tailwind CSS
- Pinia + lucide-vue-next + nprogress
- 完整的前端 API 层和类型定义

### 后续开发将采用严格最小功能点流程
从下一个功能点开始，将按照「链路 → 任务 → 功能点」的方式，逐个最小单元推进，并同步更新本文件。

---

## 提交历史

### 2026-05-17
- **分支**：`feature/frontend-v1-core-experience`
- **Commit**：保存前端 V1 核心体验（登录、资源浏览、发布、个人中心）
- **范围**：shiqian-frontend 整体重构 + 进度文档

### 2026-05-17
- **功能点**：1.2.1 实现路由守卫
- **分支**：`feature/frontend-v1-core-experience`
- **状态**：已完成
- **说明**：
  - 完善了 `beforeEach` 路由守卫
  - 支持 `requiresAuth` 和 `requiresAdmin`
  - 支持登录后 redirect 回原页面
  - 增加已登录用户访问登录/注册页自动跳转逻辑
  - 调用 `hydrateFromStorage()` 保证刷新后状态恢复

- **功能点**：1.3.1 实现退出登录
- **分支**：`feature/frontend-v1-core-experience`
- **状态**：已完成
- **说明**：
  - 在顶部导航用户下拉菜单中增加“退出登录”按钮
  - 调用 `clearSession()` 清理 Pinia 状态和 localStorage
  - 增加退出成功提示
  - 退出后跳转首页，确保路由守卫立即生效

- **功能点**：1.4.1 全局 401 拦截自动登出
- **分支**：`feature/frontend-v1-core-experience`
- **状态**：已完成
- **说明**：
  - 在 http 响应拦截器中检测 401 并派发 'auth:unauthorized' 事件
  - 在 App.vue 中监听该事件，实现自动清理状态 + 友好提示 + 跳转登录页（带 redirect）
  - 避免在登录页重复触发跳转，形成完整认证闭环

---

**下一次开发将从认证链路继续推进（路由守卫、注册完善、Token 刷新等）。**
